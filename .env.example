# === Client-facing auth ===================================================
# Oh My Pi / clients send this as: Authorization: Bearer <PROXY_API_KEY>
PROXY_API_KEY=sk-****

# === Upstream provider (server-side only, never exposed to clients) =======
OPENAI_API_KEY=sk-****
OPENAI_BASE_URL=https://api.b.ai

# === Local dev only (server.js) ===========================================
PORT=80